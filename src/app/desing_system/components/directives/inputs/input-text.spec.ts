import { InputText } from './input-text';

describe('InputText', () => {
  it('should create an instance', () => {
    const directive = new InputText();
    expect(directive).toBeTruthy();
  });
});
